<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               x:Class="Famick.HomeManagement.Mobile.Popups.NewHouseholdNamePopup"
               x:TypeArguments="x:String"
               Padding="10">

    <Border Padding="24" StrokeShape="RoundRectangle 16" Stroke="Transparent" StrokeThickness="0"
            BackgroundColor="{AppThemeBinding Light=White, Dark=#2A2A2A}" WidthRequest="320">
        <VerticalStackLayout Spacing="16">
            <Label Text="New Household" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" />
            <Label Text="Enter a name for the new household"
                   FontSize="14" HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light=#757575, Dark=#9E9E9E}" />

            <Entry x:Name="NameEntry"
                   Placeholder="Household name"
                   ReturnType="Done"
                   Completed="OnSaveClicked" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0" Text="Cancel" Clicked="OnCancelClicked"
                        BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                        TextColor="{AppThemeBinding Light=Black, Dark=White}"
                        CornerRadius="20" HeightRequest="44" />
                <Button Grid.Column="1" Text="Create" Clicked="OnSaveClicked"
                        BackgroundColor="{AppThemeBinding Light=#1976D2, Dark=#1565C0}"
                        TextColor="White" CornerRadius="20" HeightRequest="44" />
            </Grid>
        </VerticalStackLayout>
    </Border>
</toolkit:Popup>
