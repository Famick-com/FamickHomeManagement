<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Famick.HomeManagement.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:pages="clr-namespace:Famick.HomeManagement.Mobile.Pages"
    xmlns:recipes="clr-namespace:Famick.HomeManagement.Mobile.Pages.Recipes"
    xmlns:calendar="clr-namespace:Famick.HomeManagement.Mobile.Pages.Calendar"
    xmlns:contacts="clr-namespace:Famick.HomeManagement.Mobile.Pages.Contacts"
    xmlns:chores="clr-namespace:Famick.HomeManagement.Mobile.Pages.Chores"
    xmlns:household="clr-namespace:Famick.HomeManagement.Mobile.Pages.Household"
    xmlns:equipment="clr-namespace:Famick.HomeManagement.Mobile.Pages.Equipment"
    xmlns:storagebins="clr-namespace:Famick.HomeManagement.Mobile.Pages.StorageBins"
    xmlns:mealplanner="clr-namespace:Famick.HomeManagement.Mobile.Pages.MealPlanner"
    xmlns:profile="clr-namespace:Famick.HomeManagement.Mobile.Pages.Profile"
    FlyoutBehavior="Flyout"
    ios:NavigationPage.IsNavigationBarTranslucent="True">

    <!-- Household name in navigation bar -->
    <Shell.TitleView>
        <Label x:Name="TitleLabel"
               FontSize="18"
               FontAttributes="Bold"
               VerticalOptions="Center"
               TextColor="{AppThemeBinding Light=Black, Dark=White}" />
    </Shell.TitleView>

    <Shell.Resources>
        <ResourceDictionary>
            <Style TargetType="Shell" ApplyToDerivedTypes="True">
                <!-- IMPORTANT: Removing explicit background allows iOS 26 Liquid Glass -->
                <Setter Property="Shell.BackgroundColor" Value="Transparent" />
                <Setter Property="Shell.ForegroundColor" Value="{AppThemeBinding Light=Black, Dark=White}" />
                <Setter Property="Shell.TitleColor" Value="{AppThemeBinding Light=Black, Dark=White}" />
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <Shell.ItemTemplate>
        <DataTemplate>
            <Grid ColumnDefinitions="Auto,*" Padding="15,10" ColumnSpacing="15">
                <Image Source="{Binding FlyoutIcon}"
                       WidthRequest="24" HeightRequest="24"
                       VerticalOptions="Center">
                    <Image.Behaviors>
                        <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding Light=#424242, Dark=#E0E0E0}" />
                    </Image.Behaviors>
                </Image>
                <Label Grid.Column="1"
                       Text="{Binding Title}"
                       FontSize="16"
                       VerticalOptions="Center"
                       TextColor="{AppThemeBinding Light=#424242, Dark=#E0E0E0}" />
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <!-- Flyout Header: Famick Logo with Household Name -->
    <Shell.FlyoutHeader>
        <Grid HeightRequest="140" BackgroundColor="#E0DBF0C9" Padding="15">
            <VerticalStackLayout VerticalOptions="Center" Spacing="5">
                <Image Source="famick_logo"
                       Aspect="AspectFit"
                       HorizontalOptions="Center"
                       HeightRequest="70" />
                <Label x:Name="HouseholdNameLabel"
                       Text=""
                       FontSize="14"
                       HorizontalOptions="Center"
                       TextColor="{AppThemeBinding Light=#424242, Dark=#E0E0E0}" />
            </VerticalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <!-- Primary tabs: appear in BOTH flyout AND bottom tab bar -->
    <FlyoutItem FlyoutDisplayOptions="AsMultipleItems">
        <Tab Title="Home" Icon="tab_home">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate pages:DashboardPage}"
                Route="DashboardPage" />
        </Tab>

        <Tab Title="Shopping" Icon="tab_shopping">
            <ShellContent
                Title="Shopping"
                ContentTemplate="{DataTemplate pages:ListSelectionPage}"
                Route="ListSelectionPage" />
        </Tab>

        <Tab Title="Contacts" Icon="tab_contacts">
            <ShellContent
                Title="Contacts"
                ContentTemplate="{DataTemplate contacts:ContactGroupsPage}"
                Route="ContactGroupsPage" />
        </Tab>

        <Tab Title="Calendar" Icon="tab_calendar">
            <ShellContent
                Title="Calendar"
                ContentTemplate="{DataTemplate calendar:CalendarPage}"
                Route="CalendarPage" />
        </Tab>

        <Tab Title="Inventory" Icon="tab_inventory"
             Shell.BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
            <ShellContent
                Title="My Stock"
                ContentTemplate="{DataTemplate pages:StockOverviewPage}"
                Route="StockOverviewPage" />
            <ShellContent
                Title="Take Inventory"
                ContentTemplate="{DataTemplate pages:InventorySessionPage}"
                Route="InventorySessionPage" />
            <ShellContent
                Title="Products"
                ContentTemplate="{DataTemplate pages:ProductsListPage}"
                Route="ProductsListPage" />
        </Tab>

    </FlyoutItem>

    <!-- Flyout-only items (no bottom tab) -->
    <FlyoutItem Title="Household" Icon="flyout_household"
                Shell.BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
        <ShellContent
            Title="Overview"
            Icon="tab_home"
            ContentTemplate="{DataTemplate household:HouseholdOverviewPage}"
            Route="HouseholdOverviewPage" />
        <ShellContent
            Title="Utilities"
            Icon="tab_utilities"
            ContentTemplate="{DataTemplate household:HouseholdUtilitiesPage}"
            Route="HouseholdUtilitiesPage" />
        <ShellContent
            Title="Home Care"
            Icon="tab_maintenance"
            ContentTemplate="{DataTemplate household:HouseholdHomeCarePage}"
            Route="HouseholdHomeCarePage" />
        <ShellContent
            Title="Financial"
            Icon="tab_financial"
            ContentTemplate="{DataTemplate household:HouseholdFinancialPage}"
            Route="HouseholdFinancialPage" />
    </FlyoutItem>

    <FlyoutItem Title="Equipment" Icon="flyout_equipment"
                Shell.BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
        <ShellContent
            Title="Equipment"
            ContentTemplate="{DataTemplate equipment:EquipmentListPage}"
            Route="EquipmentListPage" />
    </FlyoutItem>

    <FlyoutItem Title="Storage Bins" Icon="flyout_storage_bins"
                Shell.BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
        <ShellContent
            Title="Storage Bins"
            ContentTemplate="{DataTemplate storagebins:StorageBinListPage}"
            Route="StorageBinListPage" />
    </FlyoutItem>

    <FlyoutItem Title="Chores" Icon="flyout_chores"
                Shell.BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
        <ShellContent
            Title="Chores"
            ContentTemplate="{DataTemplate chores:ChoresListPage}"
            Route="ChoresListPage" />
    </FlyoutItem>

    <FlyoutItem Title="Meal Planner" Icon="flyout_meal_planner"
                Shell.BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
        <ShellContent
            Title="Planner"
            Icon="tab_planner"
            ContentTemplate="{DataTemplate mealplanner:MealPlannerPage}"
            Route="MealPlannerPage" />
        <ShellContent
            Title="Meals"
            Icon="tab_meals"
            ContentTemplate="{DataTemplate mealplanner:MealsListPage}"
            Route="MealsListPage" />
    </FlyoutItem>

    <FlyoutItem Title="Recipes" Icon="flyout_recipes"
                Shell.BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
        <ShellContent
            Title="Recipes"
            ContentTemplate="{DataTemplate recipes:RecipeListPage}"
            Route="RecipeListPage" />
    </FlyoutItem>

    <FlyoutItem Title="Profile" Icon="flyout_profile"
                Shell.BackgroundColor="{AppThemeBinding Light=White, Dark=#1C1C1E}">
        <ShellContent
            Title="Personal Info"
            Icon="tab_person"
            ContentTemplate="{DataTemplate profile:ProfilePersonalInfoPage}"
            Route="ProfilePersonalInfoPage" />
        <ShellContent
            Title="Calendar"
            Icon="tab_calendar"
            ContentTemplate="{DataTemplate profile:ProfileCalendarPage}"
            Route="ProfileCalendarPage" />
        <ShellContent
            Title="Security"
            Icon="tab_security"
            ContentTemplate="{DataTemplate profile:ProfileSecurityPage}"
            Route="ProfileSecurityPage" />
    </FlyoutItem>

    <!-- Menu Footer with Actions -->
    <Shell.FlyoutFooter>
        <VerticalStackLayout Padding="15" Spacing="8">
            <!-- App Note -->
            <Label Text="Famick Home Management"
                   FontSize="12"
                   TextColor="{AppThemeBinding Light=#757575, Dark=#9E9E9E}"
                   HorizontalOptions="Center"
                   Margin="0,0,0,10" />

            <BoxView HeightRequest="1"
                     BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                     Margin="0,0,0,5" />

            <Button Text="Settings"
                    Clicked="OnSettingsClicked"
                    BackgroundColor="Transparent"
                    TextColor="{AppThemeBinding Light=Black, Dark=White}"
                    HorizontalOptions="Start"
                    Padding="0" />
            <Button Text="Sign Out"
                    Clicked="OnSignOutClicked"
                    BackgroundColor="Transparent"
                    TextColor="{AppThemeBinding Light=#D32F2F, Dark=#EF5350}"
                    HorizontalOptions="Start"
                    Padding="0" />
        </VerticalStackLayout>
    </Shell.FlyoutFooter>

</Shell>
