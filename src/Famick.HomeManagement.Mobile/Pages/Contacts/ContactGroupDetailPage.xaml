<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfAvatar="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:controls="clr-namespace:Famick.HomeManagement.Mobile.Controls"
             x:Class="Famick.HomeManagement.Mobile.Pages.Contacts.ContactGroupDetailPage"
             xmlns:sfButtons="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             x:Name="Page"
             Title="Group Details"
             Shell.NavBarIsVisible="True">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit"
                     Clicked="OnEditClicked"
                     IconImageSource="pencil"/>
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="*">

        <ActivityIndicator x:Name="LoadingIndicator"
                           IsRunning="True"
                           IsVisible="True"
                           VerticalOptions="Center"
                           HorizontalOptions="Center"
                           Color="{AppThemeBinding Light=#1976D2, Dark=#90CAF9}"/>

        <ScrollView x:Name="ContentScroll"
                    IsVisible="False">
            <VerticalStackLayout Spacing="16"
                                 Padding="16">

                <!-- Profile Header -->
                <VerticalStackLayout HorizontalOptions="Center"
                                     Spacing="8">
                    <Grid WidthRequest="100"
                          HeightRequest="100"
                          HorizontalOptions="Center">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnAvatarTapped"/>
                        </Grid.GestureRecognizers>
                        <sfAvatar:SfAvatarView x:Name="AvatarView"
                                               WidthRequest="100"
                                               HeightRequest="100"
                                               AvatarShape="Circle"
                                               ContentType="Initials"
                                               InitialsColor="White"
                                               Stroke="Transparent"
                                               StrokeThickness="0"
                                               FontSize="36"
                                               FontAttributes="Bold"/>
                        <!-- Edit badge overlay -->
                        <Border WidthRequest="28"
                                HeightRequest="28"
                                StrokeShape="RoundRectangle 14"
                                Stroke="{AppThemeBinding Light=White, Dark=#2A2A2A}"
                                StrokeThickness="2"
                                BackgroundColor="{AppThemeBinding Light=#1976D2, Dark=#64B5F6}"
                                HorizontalOptions="End"
                                VerticalOptions="End"
                                Margin="0,0,2,2">
                            <Label Text="&#x270E;"
                                   FontSize="14"
                                   TextColor="White"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Border>
                    </Grid>
                    <Label x:Name="GroupNameLabel"
                           FontSize="22"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"/>
                    <Border x:Name="TypeBadge"
                            Padding="8,2"
                            StrokeShape="RoundRectangle 8"
                            Stroke="Transparent"
                            HorizontalOptions="Center">
                        <Label x:Name="TypeLabel"
                               FontSize="12"
                               TextColor="White"/>
                    </Border>
                </VerticalStackLayout>

                <!-- Business Fields -->
                <VerticalStackLayout x:Name="BusinessSection"
                                     IsVisible="False"
                                     Spacing="4">
                    <Label x:Name="BusinessPhoneLabel"
                           FontSize="14"
                           TextColor="{AppThemeBinding Light=#1976D2, Dark=#64B5F6}"
                           IsVisible="False">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnBusinessPhoneTapped"/>
                        </Label.GestureRecognizers>
                    </Label>
                    <Label x:Name="WebsiteLabel"
                           FontSize="14"
                           TextColor="#1976D2">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnWebsiteTapped"/>
                        </Label.GestureRecognizers>
                    </Label>
                    <Label x:Name="CategoryLabel"
                           FontSize="13"
                           TextColor="Gray"/>
                </VerticalStackLayout>

                <!-- Addresses Section -->
                <VerticalStackLayout x:Name="AddressesSection"
                                     Spacing="8">
                    <controls:AddressSectionHeader x:Name="AddressHeader"
                                                   AddressAdded="OnContactDataChanged"/>
                    <VerticalStackLayout x:Name="AddressesLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <controls:AddressItemView DataChanged="OnContactDataChanged"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Phone Numbers Section -->
                <VerticalStackLayout x:Name="PhonesSection"
                                     Spacing="8">
                    <controls:PhoneSectionHeader x:Name="PhoneHeader"
                                                 PhoneAdded="OnContactDataChanged"/>
                    <VerticalStackLayout x:Name="PhonesLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <controls:PhoneItemView DataChanged="OnContactDataChanged"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Tags -->
                <FlexLayout x:Name="TagsLayout"
                            Wrap="Wrap"
                            JustifyContent="Start"
                            Direction="Row"/>

                <!-- Members Section -->
                <VerticalStackLayout Spacing="8">
                    <controls:GroupMemberSectionHeader x:Name="MemberHeader"
                                                       MemberAdded="OnContactDataChanged"/>
                    <VerticalStackLayout x:Name="MembersLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <controls:GroupMemberItemView GroupId="{Binding Source={x:Reference Page}, Path=GroupGuid}"
                                                              DataChanged="OnContactDataChanged"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Notes -->
                <VerticalStackLayout x:Name="NotesSection"
                                     IsVisible="False"
                                     Spacing="4">
                    <Label Text="Notes"
                           FontSize="16"
                           FontAttributes="Bold"/>
                    <Label x:Name="NotesLabel"
                           FontSize="14"
                           TextColor="Gray"/>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Error -->
        <Border x:Name="ErrorFrame"
                IsVisible="False"
                Margin="20"
                Padding="20"
                StrokeShape="RoundRectangle 10"
                Stroke="Transparent"
                BackgroundColor="#FFEBEE"
                VerticalOptions="Center">
            <VerticalStackLayout Spacing="10">
                <Label x:Name="ErrorLabel"
                       TextColor="#C62828"
                       HorizontalOptions="Center"/>
                <Button Text="Retry"
                        Clicked="OnRetryClicked"/>
            </VerticalStackLayout>
        </Border>
    </Grid>

</ContentPage>
