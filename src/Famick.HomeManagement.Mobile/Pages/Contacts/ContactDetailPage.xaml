<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfAvatar="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:sfButtons="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             xmlns:controls="clr-namespace:Famick.HomeManagement.Mobile.Controls"
             x:Class="Famick.HomeManagement.Mobile.Pages.Contacts.ContactDetailPage"
             Title="Contact"
             Shell.NavBarIsVisible="True">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit"
                Clicked="OnEditClicked"
                IconImageSource="pencil"/>
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="*">

        <ActivityIndicator x:Name="LoadingIndicator"
                           IsRunning="True"
                IsVisible="True"
                           VerticalOptions="Center"
                HorizontalOptions="Center"
                           Color="{AppThemeBinding Light=#1976D2, Dark=#90CAF9}"/>

        <ScrollView x:Name="ContentScroll"
                IsVisible="False">
            <VerticalStackLayout Spacing="16"
                    Padding="16">

                <!-- Profile Header -->
                <VerticalStackLayout HorizontalOptions="Center"
                        Spacing="8">
                    <sfAvatar:SfAvatarView x:Name="AvatarView"
                                           WidthRequest="100"
                            HeightRequest="100"
                                           HorizontalOptions="Center"
                                           AvatarShape="Circle"
                                           ContentType="Initials"
                                           BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                                           InitialsColor="{AppThemeBinding Light=#616161, Dark=#BDBDBD}"
                                           FontSize="36"
                                           FontAttributes="Bold"
                                           Stroke="Transparent"
                                           StrokeThickness="0">
                        <sfAvatar:SfAvatarView.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnProfileImageTapped"/>
                        </sfAvatar:SfAvatarView.GestureRecognizers>
                    </sfAvatar:SfAvatarView>
                    <Label x:Name="DisplayNameLabel"
                           FontSize="22"
                            FontAttributes="Bold"
                           HorizontalOptions="Center"
                            HorizontalTextAlignment="Center"/>
                    <Label x:Name="CompanyTitleLabel"
                           FontSize="14"
                            TextColor="Gray"
                           HorizontalOptions="Center"
                            IsVisible="False"/>
                    <Label x:Name="GroupNameLabel"
                           FontSize="13"
                            TextColor="#1976D2"
                           HorizontalOptions="Center"
                            IsVisible="False">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnGroupNameTapped"/>
                        </Label.GestureRecognizers>
                    </Label>
                    <Label x:Name="BirthdayLabel"
                           FontSize="13"
                            TextColor="Gray"
                           HorizontalOptions="Center"
                            IsVisible="False"/>

                    <!-- Tags -->
                    <FlexLayout x:Name="TagsLayout"
                                Wrap="Wrap"
                            JustifyContent="Center"
                                Direction="Row"
                            HorizontalOptions="Center"/>
                    <!-- Account Status (household members only) -->
                    <Label x:Name="AccountLinkLabel"
                           FontSize="13"
                            TextColor="#1976D2"
                           TextDecorations="Underline"
                           HorizontalOptions="Center"
                            IsVisible="False">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnAccountLinkTapped"/>
                        </Label.GestureRecognizers>
                    </Label>
                </VerticalStackLayout>

                <!-- Phone Numbers Section -->
                <VerticalStackLayout x:Name="PhonesSection"
                        Spacing="8">
                    <controls:PhoneSectionHeader x:Name="PhoneHeader"
                                                 PhoneAdded="OnContactDataChanged"/>
                    <VerticalStackLayout x:Name="PhonesLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <controls:PhoneItemView DataChanged="OnContactDataChanged"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Email Addresses Section -->
                <VerticalStackLayout x:Name="EmailsSection"
                        Spacing="8">
                    <controls:EmailSectionHeader x:Name="EmailHeader"
                                                 EmailAdded="OnContactDataChanged"/>
                    <VerticalStackLayout x:Name="EmailsLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <controls:EmailItemView DataChanged="OnContactDataChanged"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Addresses Section -->
                <VerticalStackLayout x:Name="AddressesSection"
                        Spacing="8">
                    <controls:AddressSectionHeader x:Name="AddressHeader"
                                                   AddressAdded="OnContactDataChanged"/>
                    <VerticalStackLayout x:Name="AddressesLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <controls:AddressItemView DataChanged="OnContactDataChanged"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Social Media Section -->
                <VerticalStackLayout x:Name="SocialSection"
                        Spacing="8">
                    <controls:SocialMediaSectionHeader x:Name="SocialHeader"
                                                       SocialAdded="OnContactDataChanged"/>
                    <VerticalStackLayout x:Name="SocialLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <controls:SocialMediaItemView DataChanged="OnContactDataChanged"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Relationships Section -->
                <VerticalStackLayout x:Name="RelationshipsSection"
                        Spacing="8">
                    <controls:RelationshipSectionHeader x:Name="RelationshipHeader"
                                                       RelationshipAdded="OnRelationshipAdded"/>
                    <VerticalStackLayout x:Name="RelationshipsLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <controls:RelationshipItemView RelationshipDeleted="OnRelationshipDeleted"/>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Notes -->
                <VerticalStackLayout x:Name="NotesSection"
                        IsVisible="False"
                        Spacing="4">
                    <Label Text="Notes"
                            FontSize="16"
                            FontAttributes="Bold"/>
                    <Label x:Name="NotesLabel"
                            FontSize="14"
                            TextColor="Gray"/>
                </VerticalStackLayout>

                <!-- Sharing Section -->
                <VerticalStackLayout x:Name="SharingSection"
                        Spacing="8">
                    <Grid ColumnDefinitions="36,*,Auto">
                        <Label Grid.Column="0"
                            Text="&#xe718;"
                               FontSize="18"
                               FontFamily="MaterialAssets"
                               FontAttributes="Bold"
                               VerticalOptions="Center"/>
                        <Label Text="Shared With"
                                FontSize="16"
                                FontAttributes="Bold"
                                VerticalOptions="Center"/>
                        <Button Grid.Column="1"
                                Text="+ Share"
                                Clicked="OnShareClicked"
                                FontSize="12"
                                HeightRequest="28"
                                Padding="10,0"
                                CornerRadius="14"
                                BackgroundColor="{AppThemeBinding Light=#1976D2, Dark=#1565C0}"
                                TextColor="White"/>
                    </Grid>
                    <VerticalStackLayout x:Name="SharesLayout">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <SwipeView>
                                    <SwipeView.RightItems>
                                        <SwipeItems>
                                            <SwipeItem Text="Remove"
                                                    BackgroundColor="#D32F2F"
                                                       Invoked="OnRemoveShareSwiped"
                                                    BindingContext="{Binding}"/>
                                        </SwipeItems>
                                    </SwipeView.RightItems>
                                    <Border Margin="0,2"
                                            Padding="10"
                                            StrokeShape="RoundRectangle 8"
                                            Stroke="Transparent"
                                            BackgroundColor="{AppThemeBinding Light=White, Dark=#2A2A2A}">
                                        <HorizontalStackLayout Spacing="8">
                                            <Label Text="{Binding SharedWithUserName}"
                                                    FontSize="15"
                                                   VerticalOptions="Center"/>
                                            <Border Padding="4,1"
                                                    StrokeShape="RoundRectangle 4"
                                                    Stroke="Transparent"
                                                    BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}">
                                                <Label FontSize="10">
                                                    <Label.Text>
                                                        <Binding Path="CanEdit"
                                                                 StringFormat="{}{0:Can Edit|View Only}"/>
                                                    </Label.Text>
                                                </Label>
                                            </Border>
                                        </HorizontalStackLayout>
                                    </Border>
                                </SwipeView>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- Audit Log Link -->
                <Button Text="View History"
                        Clicked="OnViewHistoryClicked"
                        BackgroundColor="Transparent"
                        TextColor="#1976D2"
                        HorizontalOptions="Start"
                        Padding="0"/>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Error -->
        <Border x:Name="ErrorFrame"
                IsVisible="False"
                Margin="20"
                Padding="20"
                StrokeShape="RoundRectangle 10"
                Stroke="Transparent"
                BackgroundColor="#FFEBEE"
                VerticalOptions="Center">
            <VerticalStackLayout Spacing="10">
                <Label x:Name="ErrorLabel"
                        TextColor="#C62828"
                        HorizontalOptions="Center"/>
                <Button Text="Retry"
                        Clicked="OnRetryClicked"/>
            </VerticalStackLayout>
        </Border>
    </Grid>

</ContentPage>
