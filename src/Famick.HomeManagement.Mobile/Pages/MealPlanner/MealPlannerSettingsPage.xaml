<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Famick.HomeManagement.Mobile.Pages.MealPlanner.MealPlannerSettingsPage"
             Title="Meal Planner Settings"
             Shell.NavBarIsVisible="True"
             BackgroundColor="{toolkit:AppThemeResource PageBackground}">

    <ScrollView>
        <VerticalStackLayout Spacing="0" Padding="0">

            <Label Text="Meal Planner Settings"
                   FontSize="24"
                   FontAttributes="Bold"
                   Padding="20,30,20,10"
                   TextColor="{toolkit:AppThemeResource TextPrimary}" />

            <Label Text="Configure your planning style and meal types."
                   FontSize="13"
                   Padding="20,0,20,15"
                   TextColor="{toolkit:AppThemeResource TextMuted}" />

            <!-- Loading -->
            <ActivityIndicator x:Name="LoadingIndicator" IsRunning="False" IsVisible="False"
                               HeightRequest="30" Margin="0,20"
                               Color="{toolkit:AppThemeResource BrandForeground}" />

            <!-- Planning Style Section -->
            <Label Text="Planning Style"
                   FontSize="13"
                   FontAttributes="Bold"
                   Padding="20,10,20,6"
                   TextColor="{toolkit:AppThemeResource TextMuted}" />

            <Border x:Name="StyleContainer" StrokeShape="RoundRectangle 12" Stroke="Transparent" Margin="15,0"
                    BackgroundColor="{toolkit:AppThemeResource Surface}" IsVisible="False">
                <VerticalStackLayout x:Name="StyleStack" Spacing="0" />
            </Border>

            <!-- Meal Types Section -->
            <Label Text="Meal Types"
                   FontSize="13"
                   FontAttributes="Bold"
                   Padding="20,20,20,6"
                   TextColor="{toolkit:AppThemeResource TextMuted}" />

            <Border x:Name="MealTypesContainer" StrokeShape="RoundRectangle 12" Stroke="Transparent" Margin="15,0"
                    BackgroundColor="{toolkit:AppThemeResource Surface}" IsVisible="False">
                <VerticalStackLayout x:Name="MealTypesStack" Spacing="0" />
            </Border>

            <!-- Add Meal Type Button -->
            <Button x:Name="AddMealTypeButton"
                    Text="Add Meal Type"
                    Clicked="OnAddMealTypeTapped"
                    BackgroundColor="Transparent"
                    BorderColor="{toolkit:AppThemeResource BrandForeground}"
                    BorderWidth="2"
                    TextColor="{toolkit:AppThemeResource BrandForeground}"
                    CornerRadius="8" HeightRequest="44"
                    Margin="15,15,15,30"
                    IsVisible="False" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
